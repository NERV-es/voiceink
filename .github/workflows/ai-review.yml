name: AI Review

# Thin caller. Logic lives in NERV-es/.github (SHA-pinned).
# Free GitHub Models code review on every PR. Advisory only.

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write
  models: read

jobs:
  review:
    uses: NERV-es/.github/.github/workflows/ai-review-reusable.yml@89c0d83d9607321fa644a1b36a4e64342a4cc712
    secrets: inherit
